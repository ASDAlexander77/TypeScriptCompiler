function main() {
    let a: number | string;
    let b: number | string | boolean;

    a = 10.0;

    b = a;

/*
    if (typeof(b) == "number")
    {
	print("b number: ", b);
    }

    print("done.");
*/
}