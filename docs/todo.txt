1) in code 

'''
function optstring(x: number, s?: string) {
    if (s != null) {
        return parseInt(s) + x;
    }
    return x * 2;
}
'''

s should be set to null as "llvm.udef" is not setting values which we need

