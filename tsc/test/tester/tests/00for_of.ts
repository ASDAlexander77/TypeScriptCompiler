function main() {
    for (const q of [1, 12]) {
        print(q);
    }

    for (const l of "Hello") {
        print(l);
    }

    const array1 = ['a', 'b', 'c'];

    for (const element of array1) {
        print(element);
    }

    print("done.");
}
