name: "CodeQL config"

queries:
#  - name: Run custom queries
#    uses: ./queries
  - uses: security-extended
  - uses: security-and-quality

disable-default-queries: false

#no uaws in cpp
#paths:
#  - tsc

#no uaws in cpp
#paths-ignore:
#  - __build
#  - 3rdParty